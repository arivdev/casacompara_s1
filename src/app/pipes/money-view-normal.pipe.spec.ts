import { MoneyViewNormalPipe } from './money-view-normal.pipe';

describe('MoneyViewNormalPipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyViewNormalPipe();
    expect(pipe).toBeTruthy();
  });
});

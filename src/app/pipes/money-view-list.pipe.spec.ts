import { MoneyViewListPipe } from './money-view-list.pipe';

describe('MoneyViewListPipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyViewListPipe();
    expect(pipe).toBeTruthy();
  });
});

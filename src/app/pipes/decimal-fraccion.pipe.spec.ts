import { DecimalFraccionPipe } from './decimal-fraccion.pipe';

describe('DecimalFraccionPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalFraccionPipe();
    expect(pipe).toBeTruthy();
  });
});
